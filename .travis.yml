sudo: false
language: node_js
node_js:
  - 0.10

env:
  global:
    - if [[ "$TRAVIS_TAG" != "" ]]; then BUILD_NAME=${TRAVIS_TAG}-${TRAVIS_COMMIT}.zip; else BUILD_NAME=${TRAVIS_BRANCH}-${TRAVIS_COMMIT}.zip; fi;

cache:
  directories:
    - node_modules

install: true

script: true
