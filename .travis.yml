language: objective-c
osx_image: xcode7.2

cache:
  edge: true
  directories:
    - /Library/Rubymotion
    - motion-backup

before_install:
  - sudo motion update --verbose
  - motion --version
  - which motion
  - sleep 3

install: true
script: true

before_cache:
  - mkdir motion-backup
  - sudo cp /usr/local/bin/motion motion-backup
