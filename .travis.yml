language: python
# Add or remove version for match with Travis support
python:
    - 2.6
    - 2.7
    - 3.2

os:
  - linux

matrix:
  fast_finish: true
  allow_failures:
    - os: osx
  include:
    - sudo: required
      python: 2.7
      env: ENV=lint
      before_script: TOX_ENV=lint

install: echo "Install!"

script:
  - echo $TOX_ENV
  - echo "Script!"
