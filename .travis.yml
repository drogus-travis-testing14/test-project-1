sudo: required
dist: precise

matrix:
  include:
    - sudo: required
      dist: trusty
      rvm: 2.1.2
    - sudo: false
      rvm: 2.1.2
language: ruby
rvm:
  - 2.1.2

cache: bundler

script:
  - true

notifications:
  email: false
