language: ruby

sudo: required
services:
  - docker

cache:
  directories:
    - foo

script:
  - mkdir foo
  - for i in {1..100}; do if [[ -f foo/$i ]]; then echo "I already exist"; else echo $i > foo/$i.txt; fi; done
  - echo "business ha ha"
