language: objective-c

cache:
  edge: true
  directories:
    - foo
    - bar
    - baz

before_cache:
  - cat $CASHER_DIR/md5sums_before
  - cat $CASHER_DIR/md5sums_after

install:
  - mkdir -p foo
  - mkdir -p bar
  - echo "FOOBAR" > foo/bar
  - echo "BARBAZ" > bar/baz

script:
  - which md5deep
