sudo: required
dist: precise

matrix:
  include:
    - sudo: required
      dist: trusty
    - sudo: false
language: ruby
rvm:
  - 2.1.2

cache: bundler

script:
  - true

notifications:
  email: false
