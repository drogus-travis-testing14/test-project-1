sudo: false
language: ruby
cache: bundler
rvm:
  - 2.1.6
addons:
  postgresql: "9.3"
services:
  - redis-server
before_script:
  - true
script:
  - true
deploy:
  provider: heroku
  api_key: completely_insecure
  strategy: api
  app:
    master: foobar-bar-bar
