language: objective-c

osx_image: beta-xcode6.3

cache:
  directories:
    - foo

install:
  - touch foo/bar

script:
  - I=0
  - while [ $I -lt 1200 ]; do echo .; let I=$I+1; sleep 1; done

after_script:
  - cat $CASHER_DIR/push.log
  - cat $CASHER_DIR/push.err.log
