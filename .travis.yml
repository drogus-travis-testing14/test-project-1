language: objective-c

cache:
  directories:
    - /usr/local/Cellar/xctool

script: true

before_install:
  - xctool --version
  - brew update; brew outdated xctool || brew upgrade xctool

install:
  - xctool --version
