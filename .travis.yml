language: ruby

group: dev

install:
  - true

before_script:
  - ls -l /var/run/mysqld/mysqld.sock
  - mysql --version
  - mysql -e 'create database myapp_test;'
  - sudo ls -l /var/log/mysql
  - sudo ls -l /etc/init.d/mysql && sudo cat /etc/init.d/mysql

script:
  - true
