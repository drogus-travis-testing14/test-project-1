language: ruby
cache:    bundler
script:   true
install: true

services:
  - cassandra

notifications:
  email:
    recipients:
      - hiro@travis-ci.com
    on_success: always
    on_failure: always
