sudo: false
language: ruby
cache: bundler
rvm:
  - 2.1.5
addons:
  postgresql: "9.3"
services:
  - redis-server
before_script:
  - true
script:
  - true
  
deploy:
  provider: heroku
  api_key: foobar
  app: tadada-foobar
  on:
    repo: travis-pro/test-project
    branch: master
